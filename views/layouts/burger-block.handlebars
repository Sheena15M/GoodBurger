<li>
    {{name}}
    <button class="change-devoured" data-id="{{id}}" data-newdevour="{{devour}}">
        {{#if devour}}DEVOURED{{else}}Throw Away{{/if}}
    </button>
    <button class="delete-burger" data-id="{{this.id}}">Throw Away</button>
</li>